# Student Portal

This folder will hold all student-only components (Dashboard, Progress,
Attendance, Payment, Assignments, Quiz, Profile, etc.) — built in the
next phase, the same way the teacher portal was built in `components/teacher`.

Keeping this folder separate (and empty for now) on purpose, so deleting
`components/teacher` later won't touch anything student-related, and
vice versa.
