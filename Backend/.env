PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string
MONGO_URI=mongodb://rord-00-01.gpbaqy0.mongodb.net:27017,ac-6qw2ajq-shard-00-02.gpbaqy0.mongodb.net:27017/titan_portal?ssl=true&replicaSet=atlas-1vw0ia-shard-0&authSource=admin&appName=Cluster0
# JWT
JWT_SECRET=titan_portal_super_secret_jwt_key_change_this_in_production_2026
JWT_EXPIRES_IN=7d

# Super Admin seed credentials (used once
SUPERADMIN_EMAIL=ahmedraza123@gmail.com
SUPERADMIN_PASSWORD=ahmed$123
SUPERADMIN_PHONE=03000000000
SUPERADMIN_NAME=Ahmed Raza

# When true, OTP codes are also returned in the API response + logged to the
# server console so Forgot Password can be fully tested without a real
# WhatsApp Business API account. Set to false once real WhatsApp/SMS sending
# (see backend/utils/otpService.js) is wired up.
DEV_EXPOSE_OTP=true

# Base URL the frontend uses to reach this API (used to build absolute file URLs)
BASE_URL=http://localhost:5000
